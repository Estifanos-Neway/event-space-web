export type EventPreview = {
    id: string
    title: string
    description: string
    date: Date
    bookmarks_count: number
    images: string
    city: {
        name: string
    }
    price: number
}
